name: DEV Deployment Pipeline

on:
  workflow_run:
    workflows: ["Docker Container Build Pipeline"]
    types: [completed]

jobs:
  deploy-to-dev:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy to DEV Environment
        run: |
          echo "=========================================="
          echo "🚀 Deploying Docker containers to DEV..."
          echo "1. Run Flyway Migrations"
          echo "2. Rollout Container Images tagged with Git SHA"
          echo "3. Execute Health Check & Smoke Tests"
          echo "=========================================="
