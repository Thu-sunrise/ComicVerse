server:
  port: 8089
spring:
  application:
    name: search-service
  datasource:
    url: ${SEARCH_DATABASE_URL:jdbc:postgresql://localhost:5432/search_db}
    username: ${SEARCH_DATABASE_USERNAME:postgres}
    password: ${SEARCH_DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
gateway:
  secret: ${GATEWAY_SECRET:dev-gateway-secret}
management:
  endpoints:
    web:
      exposure:
        include: health,info
